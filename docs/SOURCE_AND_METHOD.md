# 出典・方法・限界

## 一次画像
- レイヤー: 国土地理院 地理院タイル `ort_riku10`
- 表示名: 空中写真（1936年〜1942年頃）
- URL template: `https://cyberjapandata.gsi.go.jp/xyz/ort_riku10/{z}/{x}/{y}.png`
- このリポジトリのモザイク画像は、上記タイルを研究用に取得・結合した派生物である。

## 出典表示
公開画面・論文・台帳で、少なくとも次を表示する。

> 出典：国土地理院「地理院タイル（空中写真（1936年〜1942年頃））」をもとに本プロジェクトで取得・結合。

## 調査上の制約
1. `ort_riku10` は「1936年〜1942年頃」という年幅を持つレイヤーであり、セルごとの正確な撮影日を本取得処理だけで確定しない。
2. モザイク画像は候補抽出の一次資料であり、建物用途・居住者の属性・社会的地位を確定しない。
3. B001の現行範囲は既存作業から引き継いだ暫定範囲である。取得後も `scope_status=provisional_scope_needs_geographic_verification` を維持する。
4. 画像が粗い場合は、住宅・物置・農業付属施設の区別不能な「建物状影」も visual-lead として台帳化し、後工程で除外・再評価する。

## 再現性
各取得ごとに、タイルURL、HTTP状態、取得日時、SHA-256、失敗理由、対象セル、モザイク画像のハッシュを `data/manifests/` に残す。
